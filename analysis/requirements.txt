# RAGChecker Parameter-Importance-Analyse
# Python 3.10+
#
# Installation:
#   python -m venv .venv
#   source .venv/bin/activate      # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.4
shap>=0.45
optuna>=3.6
xgboost>=2.0

# dowhy unterstützt aktuell Python <= 3.12.
# Auf Python 3.13+ manuell installieren sobald ein kompatibles Release erscheint:
#   pip install dowhy
# Phase 4 (kausale Analyse) überspringt sich automatisch wenn dowhy fehlt.
# dowhy>=0.11
