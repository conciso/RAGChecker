# LightRAG API
RAGCHECKER_LIGHTRAG_URL=http://lightrag:9622
RAGCHECKER_LIGHTRAG_API_KEY=

# Query-Parameter
RAGCHECKER_QUERY_MODES=local,global,hybrid,naive,mix   # Komma-getrennte Liste der zu testenden Modi
RAGCHECKER_RUNS_PER_TESTCASE=3    # Anzahl Läufe je Testfall (für statistische Auswertung)

# Testfälle
RAGCHECKER_TESTCASES_PATH=testcases/fall1.yaml

# Reports
RAGCHECKER_OUTPUT_PATH=reports

# Modus: evaluate (normaler Testlauf) | compare (Vergleichsreport über alle Läufe)
RAGCHECKER_MODE=evaluate

# Run-Metadaten für Vergleichsreport
# RUN_GROUP: gemeinsamer Ordner für alle Presets eines Pipeline-Runs (z.B. 20260227_101000_run)
# Wird über die Kommandozeile gesetzt: RAGCHECKER_RUN_GROUP=20260227_101000_run docker compose up
RAGCHECKER_RUN_GROUP=
# RUN_LABEL: Preset-Name, überschreibt das "label"-Feld aus override.env
# Wird über die Kommandozeile gesetzt: RAGCHECKER_RUN_LABEL=baseline docker compose up
RAGCHECKER_RUN_LABEL=
# Der Wert "label" aus der override.env-Datei wird als Run-Label verwendet (sofern RAGCHECKER_RUN_LABEL nicht gesetzt)