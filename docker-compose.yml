services:
  ragchecker:
    build: .
    env_file: .env
    volumes:
      - ./testcases:/app/testcases:ro
      - ./reports:/app/reports
    networks:
      - aibox_network

networks:
  aibox_network:
    external: true
