services:
  ragchecker:
    build: .
    env_file: .env
    volumes:
      - ./testcases:/app/testcases:ro
      - ./reports:/app/reports
      - /opt/lightrag/override.env:/config/override.env:ro
    networks:
      - aibox_network

networks:
  aibox_network:
    external: true
